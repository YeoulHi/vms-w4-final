# 001. 로그인 및 대시보드 접근 - 종합 테스트 케이스 (Test Cases)

**문서 버전:** 1.0
**작성일:** 2025-10-31
**범위:** UserFlow #01 및 관련 기능

---

## 📋 개요

본 문서는 "로그인 및 대시보드 접근" 기능의 모든 테스트 케이스를 정의합니다.

**참고 문서:**
- `docs/spec/001-spec-로그인-및-대시보드-접근.md`
- `docs/spec/001-plan-로그인-및-대시보드-접근.md`
- `docs/log/001-로그인_및_대시보드_접근_교차검증.md`

---

## ✅ TC-01: 비로그인 상태 대시보드 접근

### 사전 조건
- 사용자가 로그인되지 않은 상태
- 쿠키 또는 세션이 없음

### 테스트 단계
1. 브라우저에서 `/dashboard/` URL에 직접 접근
2. 또는 루트 경로 `/`에 접근

### 예상 결과
- **상태 코드:** 302 (Redirect) 또는 301 (Permanent Redirect)
- **리다이렉트 경로:** `/login/?next=/dashboard/`
- **설명:** 비로그인 사용자는 즉시 로그인 페이지로 리다이렉트됨

### 현재 상태
✅ **완료** - `@login_required` 데코레이터에 의해 구현됨

### 구현 파일
- `apps/dashboard/views.py:20-31` - `DashboardView` 클래스

### 검증 방법
```bash
# 비로그인 상태로 대시보드 접근
curl -i http://localhost:8000/dashboard/
# 응답: 302 Location: http://localhost:8000/login/?next=/dashboard/

# 또는 브라우저에서:
# 1. 브라우저 개발자 도구 열기 (F12)
# 2. 네트워크 탭에서 302 리다이렉트 확인
```

---

## ✅ TC-02: 로그인 후 로그인 페이지 재접근

### 사전 조건
- 사용자가 유효한 계정으로 로그인됨
- 세션 쿠키가 발급됨

### 테스트 단계
1. 유효한 ID/PW로 `/login/` 페이지에서 로그인
2. 로그인 성공 후
3. 다시 `/login/` URL에 직접 접근

### 예상 결과
- **상태 코드:** 302 (Redirect)
- **리다이렉트 경로:** `/dashboard/`
- **설명:** 이미 로그인된 사용자가 로그인 페이지에 접근하면 대시보드로 리다이렉트됨

### 현재 상태
✅ **완료** - `LoginView` 설정에서 `redirect_authenticated_user=True`에 의해 구현됨

### 구현 파일
- `config/urls.py:14-19` - `LoginView` 구성

### 검증 방법
```bash
# 로그인 (username, password는 실제 테스트 계정 사용)
curl -X POST http://localhost:8000/login/ \
  -d "username=testuser&password=testpass" \
  -c cookies.txt

# 로그인 후 다시 /login/ 접근
curl -i -b cookies.txt http://localhost:8000/login/
# 응답: 302 Location: http://localhost:8000/dashboard/
```

---

## ✅ TC-04: 잘못된 ID/PW로 로그인 시도

### 사전 조건
- 사용자가 로그인되지 않은 상태
- 정확한 ID/PW를 모름

### 테스트 단계
1. `/login/` 페이지 접근
2. 잘못된 ID 또는 비밀번호 입력
3. '로그인' 버튼 클릭

### 예상 결과
- **상태 코드:** 200 (OK)
- **페이지:** 로그인 폼 재표시
- **에러 메시지:** "Invalid username or password." 또는 유사 메시지
- **설명:** 폼 유효성 검사 실패 시 에러 메시지가 표시되고 로그인 폼이 다시 렌더링됨

### 현재 상태
✅ **완료** - Django 기본 폼 에러 처리 구현

### 구현 파일
- `templates/registration/login.html` - 폼 에러 표시 로직
- `config/urls.py:14-19` - `LoginView`

### 검증 방법
```bash
# 잘못된 ID/PW로 로그인 시도
curl -X POST http://localhost:8000/login/ \
  -d "username=invaliduser&password=wrongpass" \
  -L

# 응답에 에러 메시지 포함 확인
# <div class="errorlist">...</div> 태그 확인
```

---

## ❌ TC-03: 비로그인 상태 API 호출 (NEW)

### 사전 조건
- 사용자가 로그인되지 않은 상태
- DB에 MetricRecord 데이터 존재

### 테스트 단계
1. 세션 쿠키 없이 `/api/dashboard/chart-data/` API 호출
2. 선택적 쿼리 파라미터: `year=2024&department=컴퓨터공학과`

### 예상 결과
- **상태 코드:** 401 (Unauthorized) 또는 302 (Redirect to /login/)
- **응답 본문:**
  - Redirect 경우: HTML 로그인 페이지
  - 또는 JSON: `{"error": "unauthorized"}`
- **설명:** 비로그인 사용자는 API 접근이 거부됨

### 현재 상태
🆕 **새로 구현됨** (이전에는 API가 없었음)

### 구현 파일
- `apps/dashboard/views.py:58-109` - `ChartDataAPIView` 클래스
- `apps/dashboard/urls.py:15` - API URL 매핑

### 검증 방법
```bash
# 비로그인 상태로 API 호출
curl -i http://localhost:8000/api/dashboard/chart-data/?year=2024
# 응답: 302 Location: http://localhost:8000/login/?next=/api/dashboard/chart-data/...

# 또는 JSON 응답 설정 시:
# 응답: 401 Unauthorized
```

---

## ✅ TC-03-1: 로그인 후 API 호출 성공

### 사전 조건
- 사용자가 유효한 계정으로 로그인됨
- DB에 MetricRecord 데이터 존재 (year=2024, department="컴퓨터공학과")

### 테스트 단계
1. 유효한 세션으로 `/api/dashboard/chart-data/` 호출
2. 쿼리 파라미터: `year=2024&department=컴퓨터공학과`

### 예상 결과
- **상태 코드:** 200 (OK)
- **Content-Type:** `application/json`
- **응답 본문:**
```json
{
  "labels": ["2024"],
  "datasets": [
    {
      "label": "PAPER",
      "data": [15],
      "backgroundColor": "#4A90E2"
    }
  ]
}
```
- **설명:** Chart.js 호환 JSON 형식으로 데이터 반환

### 현재 상태
🆕 **새로 구현됨**

### 구현 파일
- `apps/dashboard/services.py` - `get_dashboard_data()`, `to_chartjs()`
- `apps/dashboard/views.py:68-102` - API 로직

### 검증 방법
```bash
# 먼저 로그인하여 세션 쿠키 획득
curl -X POST http://localhost:8000/login/ \
  -d "username=testuser&password=testpass" \
  -c cookies.txt

# 그 후 API 호출
curl -i -b cookies.txt "http://localhost:8000/api/dashboard/chart-data/?year=2024&department=컴퓨터공학과"
# 응답: 200 OK + JSON
```

---

## ⚠️ TC-05: 관리자 페이지 `is_staff` 권한 검증

### 사전 조건
- 일반 사용자 계정 (is_staff=False)으로 로그인됨
- 또는 로그인되지 않은 상태

### 테스트 단계
1. 일반 사용자 계정으로 로그인
2. `/admin/` 또는 `/admin/ingest/metricrecord/` 접근 시도

### 예상 결과
- **상태 코드:** 403 (Forbidden) 또는 302 (Redirect to /login/)
- **페이지:** "You don't have permission to access this resource." 오류 페이지
- **설명:** Django Admin은 자체 권한 검증 로직으로 is_staff=False 사용자 접근 차단

### 현재 상태
✅ **완료** - Django Admin 기본 동작 + `MetricRecordAdmin` 명시적 권한 검증 추가

### 구현 파일
- `apps/ingest/admin.py:59-81` - 명시적 `has_*_permission()` 메서드
- Django Admin 기본 인증 (자동)

### 검증 방법
```bash
# 일반 사용자 계정으로 로그인
curl -X POST http://localhost:8000/login/ \
  -d "username=normaluser&password=testpass" \
  -c cookies.txt

# Admin 접근 시도
curl -i -b cookies.txt http://localhost:8000/admin/
# 응답: 302 (로그인 안 됨) 또는 403 (로그인됨, 권한 없음)

# 관리자 계정으로 로그인해 비교
curl -X POST http://localhost:8000/login/ \
  -d "username=admin&password=adminpass" \
  -c admin_cookies.txt

curl -i -b admin_cookies.txt http://localhost:8000/admin/
# 응답: 200 OK
```

---

## 📊 테스트 실행 계획

### 1단계: 단위 테스트 (Unit Tests)

```bash
# Django 내장 테스트 실행
python manage.py test apps.dashboard.tests
python manage.py test apps.ingest.tests
```

### 2단계: 통합 테스트 (Integration Tests)

```bash
# 전체 기능 테스트
python manage.py test
```

### 3단계: 수동 테스트 (Manual QA)

#### 3-1. 로컬 서버 시작
```bash
python manage.py runserver
```

#### 3-2. 각 TC 실행
- 브라우저에서 `/dashboard/` 접근 → TC-01 확인
- 로그인 후 `/login/` 재접근 → TC-02 확인
- 잘못된 ID/PW → TC-04 확인
- API 호출 (비로그인) → TC-03 확인
- API 호출 (로그인) → TC-03-1 확인
- Admin 접근 → TC-05 확인

---

## 🔍 API 엔드포인트 상세 명세

### GET `/api/dashboard/chart-data/`

#### 요청 파라미터

| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|------|------|
| year | integer | N | 필터링할 연도 (예: 2024) |
| department | string | N | 필터링할 학과 이름 |

#### 응답 (200 OK)

```json
{
  "labels": ["2023", "2024", "2025"],
  "datasets": [
    {
      "label": "PAPER",
      "data": [10, 15, 20],
      "backgroundColor": "#4A90E2"
    },
    {
      "label": "BUDGET",
      "data": [5000, 6000, 7000],
      "backgroundColor": "#50E3C2"
    }
  ]
}
```

#### 응답 (400 Bad Request)

잘못된 파라미터 입력 시:

```json
{
  "error": "invalid_parameter"
}
```

**상황:**
- `year` 파라미터가 정수가 아님
- 예: `/api/dashboard/chart-data/?year=abc`

#### 응답 (401/403 Unauthorized/Forbidden)

`@login_required` 데코레이터에 의해 처리됨:
- 비로그인: 302 Redirect to `/login/`
- 또는 API 클라이언트: 401 응답

#### 응답 (500 Internal Server Error)

예상치 못한 에러 발생:

```json
{
  "error": "server_error"
}
```

---

## 📝 테스트 결과 기록

### TC-01 ✅
- 상태: 통과
- 날짜: -
- 비고: -

### TC-02 ✅
- 상태: 통과
- 날짜: -
- 비고: -

### TC-03 🆕
- 상태: 신규 구현 (테스트 필요)
- 날짜: -
- 비고: ChartDataAPIView 구현 완료

### TC-03-1 ✅
- 상태: 신규 구현 (테스트 필요)
- 날짜: -
- 비고: API 정상 동작 확인 필요

### TC-04 ✅
- 상태: 통과
- 날짜: -
- 비고: -

### TC-05 ✅
- 상태: 통과 (명시화됨)
- 날짜: -
- 비고: `MetricRecordAdmin` 권한 메서드 추가

---

## 🎯 구현 완료 체크리스트

### 기능 구현
- ✅ `DashboardView` - `@login_required` 적용
- ✅ `ChartDataAPIView` - 새로운 API 엔드포인트
- ✅ `get_dashboard_data()` - DB 조회 서비스
- ✅ `to_chartjs()` - 데이터 변환 서비스
- ✅ `MetricRecordAdmin` - 관리자 인터페이스 권한 제어

### URL 라우팅
- ✅ `/dashboard/` → `DashboardView`
- ✅ `/api/dashboard/chart-data/` → `ChartDataAPIView`
- ✅ `/login/` → `LoginView` (redirect_authenticated_user=True)
- ✅ `/logout/` → `LogoutView`
- ✅ `/` → RedirectView (/dashboard/)

### 문서화
- ✅ `docs/log/001-로그인_및_대시보드_접근_교차검증.md` - 초기 교차검증
- ✅ `docs/log/001-test-로그인및대시보드_테스트케이스.md` - 본 문서 (종합 테스트)

---

## 🔗 관련 문서

- [PRD](../3.prd.md) - 제품 요구사항 명세
- [UserFlow](../4.userflow.md) - 사용자 여정
- [DataFlow](../5.dataflow.md) - 데이터 흐름
- [Spec](001-spec-로그인-및-대시보드-접근.md) - 유스케이스 명세
- [Implementation Plan](001-plan-로그인-및-대시보드-접근.md) - 구현 계획

---

## 📌 다음 단계

1. **테스트 자동화:** `tests/test_001_login_dashboard.py` 작성
2. **에러 핸들링 강화:** 예외 처리 및 로깅 추가
3. **성능 테스트:** API 응답 시간 측정 (목표: 500ms 이내)
4. **보안 검증:** CSRF, XSS 방지 확인

---

**버전 이력**

| 버전 | 날짜 | 작성자 | 변경사항 |
|------|------|--------|---------|
| 1.0 | 2025-10-31 | Claude | 초기 작성, TC-01~05 정의 |
